{
  "name": "cadeau-de-claude",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for multiple applications",
  "scripts": {
    "dev": "pnpm --filter music-sheets dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
